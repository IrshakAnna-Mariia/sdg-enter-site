export const SectionHeader: React.FC<{ title: string }> = ({ title }) => {
  return <h2 className="font-chakraPetch text-5xl text-center font-bold text-white">{title}</h2>;
};

export default SectionHeader;
